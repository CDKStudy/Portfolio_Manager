{
  "name": "portfolio-manager-backend",
  "version": "1.0.0",
  "description": "Portfolio Manager REST API",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "test": "jest",
    "test:stocks": "jest tests/stock-operations.test.js --verbose",
    "test:simple": "node test-stocks.js",
    "db:setup": "mysql -u root -p < database/setup.sql",
    "db:reset": "mysql -u root -p -e \"DROP DATABASE IF EXISTS portfolio_manager; CREATE DATABASE portfolio_manager;\" && npm run db:setup"
  },
  "keywords": [
    "portfolio",
    "finance",
    "api"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@tensorflow/tfjs": "^4.22.0",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "yahoo-finance2": "^2.8.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.5",
    "@types/node": "^20.8.0",
    "jest": "^29.7.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
